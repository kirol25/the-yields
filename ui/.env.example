# Copy this file to .env.local and fill in your values
# .env.local is gitignored - never commit real values

# ── Backend ───────────────────────────────────────────────────────────────────
VITE_API_BASE=http://localhost:8000

# ── Auth mode ────────────────────────────────────────────────────────────────
# 'local' auto-logs-in as a fake dev user and skips Cognito. Must match
# AUTH_MODE on the backend. Set to 'cognito' for any non-local deployment.
VITE_AUTH_MODE=local

# ── AWS Cognito ───────────────────────────────────────────────────────────────
# App client ID from the Cognito User Pool (required when VITE_AUTH_MODE=cognito)
VITE_COGNITO_CLIENT_ID=client_id

# ── Feature flags ────────────────────────────────────────────────────────────
# Set to true to allow new user registrations
VITE_REGISTRATION_ENABLED=true

# ── AWS Console prerequisite ──────────────────────────────────────────────────
# Enable USER_PASSWORD_AUTH on the App Client:
# Cognito → User Pools → App clients → Edit → Authentication flows
# Check "ALLOW_USER_PASSWORD_AUTH"
