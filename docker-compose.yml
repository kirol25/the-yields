services:
  backend:
    build: ./backend
    env_file:
      - ./backend/.env
    volumes:
      - ./data:/data
    environment:
      CORS_ORIGINS: http://localhost
    ports:
      - "127.0.0.1:8000:8000"
    labels:
      app: "the-yields"
      service: "backend"
  ui:
    build:
      context: ./ui
    env_file:
      - ./ui/.env
    ports:
      - "127.0.0.1:3000:8080"
    depends_on:
      - backend
    labels:
      app: "the-yields"
      service: "ui"
